CREATE SCHEMA dev;
CREATE TABLE dev.transactional_outbox (
  id INT GENERATED BY DEFAULT AS IDENTITY,
  topic BYTEA NOT NULL,
  key BYTEA NULL,
  value BYTEA NOT NULL,
  published_ts TIMESTAMPTZ NULL
);